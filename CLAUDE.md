# 명령어는 Bash 로
파워쉘 금지, 필요 시 요청

# 정적 검증
`npm.cmd run lint` (= eslint www/js)

# 명령어
## `cd` 로 경로 명시 생략 (이미 작업 디렉토리)

## 1명령어 1실행 원칙
`&&` 등으로 한번에 여러개 처리 가급적 지양

# 시각 검증은 사람이
프리뷰 띄워서 검증 안해도 됨

# 캔바스
`lineCap` 정의 금지. 무조건 기본값